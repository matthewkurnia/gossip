WHITESPACE = _{ " " }
COMMENT    = _{ "/*" ~ (!"*/" ~ ANY)* ~ "*/" }

name  = ${ (CASED_LETTER | " ")+ }
names =  { NEWLINE* ~ (name ~ NEWLINE+)* }
file  =  { SOI ~ (names ~ ("\r\n" | "\n"))* ~ EOI }
